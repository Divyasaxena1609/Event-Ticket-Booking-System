spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/event_db
    username: postgres
    password: Aman@2209

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
