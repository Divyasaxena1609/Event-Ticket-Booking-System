spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/booking_db
    username: postgres
    password: Divya@1609

  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

razorpay:
  key-id: rzp_test_Surl2yhYzE8VAv
  key-secret: VyBWSQPvc4KF7uwe3bsDe0Iw
  webhook-secret: dev_webhook_secret_123

services:
  user:
    url: http://localhost:8085
