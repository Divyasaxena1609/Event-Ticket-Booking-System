spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/booking_db
    username: postgres
    password: ${BOOKING_DB_PASSWORD:Divya@1609}

  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

razorpay:
  key-id: ${RAZORPAY_KEY_ID:rzp_test_Surl2yhYzE8VAv}
  key-secret: ${RAZORPAY_KEY_SECRET:VyBWSQPvc4KF7uwe3bsDe0Iw}
  webhook-secret: ${RAZORPAY_WEBHOOK_SECRET:dev_webhook_secret_123}

seat-lock:
  allow-in-memory-fallback: true

services:
  event:
    url: ${EVENT_SERVICE_URL:http://localhost:8083}
  user:
    url: http://localhost:8085
